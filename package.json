{
  "name": "lmjtfy",
  "version": "1.0.0",
  "description": "Let Me Jev That For You \u2014 ask Jev a yes or no question.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate:local": "wrangler d1 migrations apply lmjtfy --local",
    "db:migrate": "wrangler d1 migrations apply lmjtfy --remote",
    "tail": "wrangler tail",
    "build:logo": "node scripts/build-logo.mjs",
    "build:og": "node scripts/build-og.mjs"
  },
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@typesafe-ai/sdk": "^0.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "@posthog/brand": "^0.12.2",
    "playwright-core": "^1.63.0",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1",
    "wrangler": "^4.135.0"
  },
  "private": true
}
